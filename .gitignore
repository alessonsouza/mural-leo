# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variáveis de ambiente — contêm senhas e chaves, nunca devem ir para o Git.
.env
!.env.example

# Override local do docker-compose (ajustes específicos da máquina de cada dev,
# como portas remapeadas). Não deve ir para o repositório.
docker-compose.override.yml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
